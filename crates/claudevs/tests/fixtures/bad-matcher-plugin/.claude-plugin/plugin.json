{
  "name": "bad-matcher-plugin",
  "version": "0.1.0",
  "description": "claudevs negative fixture: an unknown event, an uncompilable matcher (both warnings), and a reference to a script that does not exist (an error, so wiring still fails)",
  "author": { "name": "rstlix0x0" }
}
